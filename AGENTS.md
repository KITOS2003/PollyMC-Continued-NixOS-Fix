## Critical Rules
- **NEVER push to any remote without explicitly asking the user first.** Wait for a clear "push" / "go ahead" / "yes push".
- Private repo remote is named `build`; public repo is `origin`. Ask which one before pushing.
